# Fix E2E for PR #82 (unified import/export modal)

## Problem context and approach

CI e2e failures in PR #82 after adding unified import/export modal and moving audio import. Failures include:

- Playwright strict-mode violation on file input (multiple `input[type="file"]` under `settings-dialog`).
- Project rename expectations failing (page title and list text).

Approach:

- Inspect updated UI structure to see how modal/inputs changed.
- Update e2e helpers/selectors to target the new import/export modal and file inputs reliably.
- Update rename tests if behavior/labels legitimately changed; otherwise fix UI to restore expected behavior.

## Reference files/patterns to follow

- e2e helpers: `e2e/helpers.ts`
- Failing tests: `e2e/multiple-projects.spec.ts`, `e2e/mute-shortcuts.spec.ts`, `e2e/transport.spec.ts`
- Import/export UI: `src/components/import-export.tsx` (or similar)
- Dialog UI: `src/components/ui/dialog.tsx`
- App/transport wiring: `src/app.tsx`, `src/components/transport.tsx`

## Implementation steps

1. Reproduce/understand failures locally (read code + update selectors).
2. Fix file input selection in e2e helpers to target the correct dialog/input.
3. Fix rename tests or UI behavior depending on expected state (rename cancel, rename from main app).
4. Run targeted e2e specs to verify.

## Feedback log

- 2026-02-01: User requested fixing e2e failures on PR #82.

## Status

- What's done: added test IDs for audio/midi file inputs, updated e2e audio loader selector, fixed rename cancel behavior, updated main-app rename e2e flow.
- What's remaining: run targeted e2e to confirm (mute-shortcuts, transport, multiple-projects).
- Blockers/open questions: none.
